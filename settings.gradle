pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "OpenPayExam"
include ':app'
include ':feature:dashboard'
include ':connection:retrofit'
include ':tools:entities_adapter'
include ':use_case:dashboard'
include ':connection:room'
